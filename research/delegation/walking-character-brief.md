Create ONLY a new app/components/personal/WalkingCharacter.jsx in sri_portfolio/sri_portfolio. Main is writing walker controller/network concurrently; don't edit those or any other files. Use existing Character.jsx body/face/wardrobe and CharacterArm style, residentLeg from app/lib/resident-motion.mjs for grounded articulated gait. Export default WalkingCharacter({motion,color}), where motion is React ref with current {gait:number,stride:number}; root local +Y up, +Z facing; height approx1.65 meters, feet at y0. New component may reuse Character via new walking prop only if absolutely necessary, but prefer self-contained reuse of CharacterFace, Shirt, HikingPack, CharacterArm, Shoe. Avoid copying huge geometry; shared basic model components. Use ref-driven useFrame updates; no per-frame React setState; when stride=0 idle feet are onground, arms settle. Muted colored jacket with backpack marks visitor; visual consistency. Keep limb lengths stable using existing residentLeg two-bone endpoints and segments oriented accordingly, no float/swing whole rigid legs. Roottransform ownedby main. Keepgeometryreuse+dispose correct, no extra lights. Read relevant AGENTS. Report changedfile +validation to research/delegation/walking-character-result.md. Run npm run check. No browser tools, no deploy, no other changes. Use grok4.7xhigh alreadyset. Bounded quick unit, no broaderqueue reading.
